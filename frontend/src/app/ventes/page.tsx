export default function VentesPage() {
  return (
    <section>
      <h1>Ventes</h1>
      <p className="muted">
        Formulaire de saisie des ventes — à construire (Imma), via
        <code> api.sales.create</code>.
      </p>
      <div className="card">SKU produit, quantité, prix unitaire, date</div>
    </section>
  );
}

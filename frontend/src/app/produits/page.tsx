export default function ProduitsPage() {
  return (
    <section>
      <h1>Produits</h1>
      <p className="muted">
        Formulaire de saisie des produits — à construire (Imma), via
        <code> api.products.create</code>.
      </p>
      <div className="card">SKU, nom, catégorie, coût, prix, stock, seuil d’alerte</div>
    </section>
  );
}

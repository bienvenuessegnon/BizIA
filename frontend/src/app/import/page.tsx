export default function ImportPage() {
  return (
    <section>
      <h1>Import CSV / Excel</h1>
      <p className="muted">
        Écran d’import — à construire (Imma), via <code>api.ingestFile</code>.
      </p>
      <div className="card">
        Les fichiers importés rejoignent le même schéma commun que la saisie manuelle.
      </div>
    </section>
  );
}
